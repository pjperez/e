{
  "//": "Copy to ~/.e/mcp.json (every project) or <project>/.e/mcp.json (this one). Same-named servers: the project entry wins.",
  "servers": {
    "files": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "//cwd": "Defaults to the project folder, so \".\" above means this project."
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_replace_me" }
    },
    "parked": {
      "command": "npx",
      "args": ["-y", "some-server"],
      "disabled": true
    }
  }
}
